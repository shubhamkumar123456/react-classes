import React from 'react'

const Login = () => {

    // res  --> {msg:"login successfull" ,user:{id, name, email, password}};

    localStorage. save(user)
    Navigate('/')

  return (
    <div>
      <h1>This is loginpage</h1>
    </div>
  )
}

export default Login
